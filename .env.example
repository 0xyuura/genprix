# Copy to .env.local and fill in to enable SECURE MODE (global leaderboard + server-authoritative
# scoring + admin question editor). Leave unset for local/demo mode (localStorage board).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
